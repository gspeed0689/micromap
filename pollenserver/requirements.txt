setuptools  # Packaging and installation
fastapi>=0.88.0  # API framework
pydantic_settings>=2.0.0  # Data validation and settings management
uvicorn>=0.20.0  # Minimal low-level server/application interface
a2wsgi>=1.7.0  # Required for WSGI support (passenger, gunicorn)
